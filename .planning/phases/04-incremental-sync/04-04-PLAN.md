---
phase: 04-incremental-sync
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/folders/index.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Folder selection shows already-selected folders as pre-selected in the list"
  artifacts:
    - path: "src/folders/index.ts"
      provides: "selectFolders with initialValues support"
      contains: "initialValues"
  key_links:
    - from: "syncFolderConfig()"
      to: "selectFolders()"
      via: "passes trackedFolderIds as currentSelection"
      pattern: "selectFolders\\(folders,.*trackedFolderIds"
---

<objective>
Fix folder selection to pre-select already-tracked folders when using --select flag.

Purpose: When user runs `folders --select`, currently tracked folders should appear pre-selected in the multiselect prompt, not blank.
Output: Updated selectFolders() that accepts and passes initialValues to @clack/prompts multiselect.
</objective>

<execution_context>
@/Users/i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/folders/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add initialValues support to selectFolders</name>
  <files>src/folders/index.ts</files>
  <action>
  Two changes needed:

  1. Update selectFolders() function signature (line 76):
     - FROM: `export async function selectFolders(folders: FolderInfo[]): Promise<number[]>`
     - TO: `export async function selectFolders(folders: FolderInfo[], currentSelection?: number[]): Promise<number[]>`

  2. Add initialValues to multiselect options (line 77-84):
     - Add `initialValues: currentSelection` to the multiselect options object
     - Place it after `required: true`

  The multiselect call should become:
  ```typescript
  const selected = await multiselect({
    message: 'Select folders to track:',
    options: folders.map(f => ({
      value: f.id,
      label: `${f.title} (${f.chatCount} chats)`
    })),
    required: true,
    initialValues: currentSelection
  })
  ```

  3. Update syncFolderConfig() call to selectFolders (line 140):
     - FROM: `trackedFolderIds = await selectFolders(folders)`
     - TO: `trackedFolderIds = await selectFolders(folders, trackedFolderIds)`

  Wait, that's wrong - trackedFolderIds isn't defined yet at that point.
  The existing tracked folders are in `config.trackedFolders`.

  Correct change at line 140:
     - FROM: `trackedFolderIds = await selectFolders(folders)`
     - TO: `trackedFolderIds = await selectFolders(folders, Object.keys(config.trackedFolders).map(Number))`

  This passes the existing tracked folder IDs as initialValues when forceSelect=true.
  On first run (isFirstRun=true), config.trackedFolders is empty, so initialValues will be [].
  </action>
  <verify>
  ```bash
  npx tsc --noEmit
  ```
  Should compile without errors.
  </verify>
  <done>
  - selectFolders() accepts optional currentSelection parameter
  - multiselect() receives initialValues from currentSelection
  - syncFolderConfig() passes existing folder IDs when calling selectFolders with forceSelect
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Manual test: Run `npm run dev -- folders --select` with existing config - previously tracked folders should appear pre-selected
</verification>

<success_criteria>
- Running `folders --select` shows multiselect with previously tracked folders already checked
- First run (no config) still works - shows all folders unchecked
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-incremental-sync/04-04-SUMMARY.md`
</output>
