---
phase: 03-core-message-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/fetch.ts
  - src/utils/filename.ts
autonomous: true

must_haves:
  truths:
    - "Messages are fetched in chunks of 100 with 1.5s+jitter delay between batches"
    - "Chat names are sanitized for filesystem use with fallback to chat ID"
  artifacts:
    - path: "src/messages/fetch.ts"
      provides: "Message fetching with rate limiting"
      exports: ["fetchMessages"]
    - path: "src/utils/filename.ts"
      provides: "Filename sanitization"
      exports: ["sanitizeFilename"]
  key_links:
    - from: "src/messages/fetch.ts"
      to: "@mtcute/node"
      via: "tg.iterHistory"
      pattern: "iterHistory.*chunkSize"
---

<objective>
Create utility modules for message fetching with rate limiting and filename sanitization.

Purpose: Establish the foundational utilities that the message export pipeline depends on.
Output: Two independent utility modules (fetch.ts, filename.ts) ready for use by the formatting and writing layers.
</objective>

<execution_context>
@/Users/i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-message-export/03-RESEARCH.md

@src/client.ts
@src/config/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create message fetch module with rate limiting</name>
  <files>src/messages/fetch.ts</files>
  <action>
Create src/messages/fetch.ts with an async generator function that wraps tg.iterHistory with rate limiting.

Implementation:
1. Export an async generator `fetchMessages(tg: TelegramClient, chatId: number, onProgress?: (count: number) => void)`
2. Use `tg.iterHistory(chatId, { chunkSize: 100 })` to stream messages
3. After every 100 messages, call `onProgress` if provided and wait 1500-2000ms (1.5s + random 0-500ms jitter)
4. Yield each message as-is for downstream processing
5. Export a helper `sleep(ms: number)` for the delay

Note: Messages come newest-first from iterHistory. The writer (Plan 02) will handle reversal.

Use ES module imports with .js extensions per project convention.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
File exists at src/messages/fetch.ts
  </verify>
  <done>
fetchMessages generator yields messages from iterHistory with 1.5s+jitter delay every 100 messages
  </done>
</task>

<task type="auto">
  <name>Task 2: Create filename sanitization utility</name>
  <files>src/utils/filename.ts</files>
  <action>
Create src/utils/filename.ts with aggressive filename sanitization for cross-platform safety.

Implementation:
1. Export `sanitizeFilename(name: string, fallbackId?: number): string`
2. Remove characters invalid on Windows/Mac/Linux: `<>:"/\|?*` and control characters (0x00-0x1f, 0x80-0x9f)
3. Collapse multiple spaces to single space and trim
4. Remove trailing dots (Windows issue)
5. Check for Windows reserved names (con, prn, aux, nul, com1-9, lpt1-9) and prefix with underscore
6. Truncate to 200 characters max (leave room for path + .md extension)
7. If result is empty, return `chat-{fallbackId}` or `unnamed` if no fallbackId

Reference from research:
```typescript
const INVALID_CHARS = /[<>:"/\\|?*\x00-\x1f\x80-\x9f]/g
const WINDOWS_RESERVED = /^(con|prn|aux|nul|com[1-9]|lpt[1-9])$/i
```
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
File exists at src/utils/filename.ts
  </verify>
  <done>
sanitizeFilename handles all edge cases: special chars removed, Windows reserved names prefixed, length truncated, fallback to ID when empty
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Both files exist and export their functions
3. Imports use .js extensions for ESM compatibility
</verification>

<success_criteria>
- src/messages/fetch.ts exports fetchMessages async generator with rate limiting
- src/utils/filename.ts exports sanitizeFilename with comprehensive sanitization
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-message-export/03-01-SUMMARY.md`
</output>
