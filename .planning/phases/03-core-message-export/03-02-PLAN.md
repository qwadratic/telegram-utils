---
phase: 03-core-message-export
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/messages/format.ts
  - src/messages/writer.ts
autonomous: true

must_haves:
  truths:
    - "Messages include sender info with name and @username"
    - "Replies show quoted snippet of original message"
    - "Forwards are marked with source attribution"
    - "Attachments are marked with type (no media download)"
    - "Text formatting (bold, italic, links, code) is preserved as Markdown"
    - "Files are written to archive/YYYY-MM/chat-name.md structure"
    - "Each file has YAML frontmatter with chat metadata"
  artifacts:
    - path: "src/messages/format.ts"
      provides: "Message-to-Markdown formatting"
      exports: ["formatMessage", "formatSender"]
    - path: "src/messages/writer.ts"
      provides: "Monthly file writer with frontmatter"
      exports: ["writeMonthlyFiles", "groupByMonth"]
  key_links:
    - from: "src/messages/format.ts"
      to: "@mtcute/markdown-parser"
      via: "md.unparse"
      pattern: "md\\.unparse"
    - from: "src/messages/writer.ts"
      to: "src/utils/filename.ts"
      via: "sanitizeFilename import"
      pattern: "import.*sanitizeFilename"
---

<objective>
Create message formatting and file writing modules for the export pipeline.

Purpose: Transform raw Telegram messages into readable Markdown and write them to monthly archive files.
Output: Format module that converts messages to Markdown, writer module that handles monthly file structure with YAML frontmatter.
</objective>

<execution_context>
@/Users/i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-message-export/03-RESEARCH.md
@.planning/phases/03-core-message-export/03-01-PLAN.md

@src/utils/filename.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create message formatting module</name>
  <files>src/messages/format.ts</files>
  <action>
Create src/messages/format.ts to convert Telegram messages to Markdown.

Implementation:

1. Export `formatSender(sender: User | Chat | AnonymousSender): string`
   - Handle `sender.type === 'anonymous'`: return `sender.displayName`
   - Handle `sender.type === 'user'`: format as "FirstName LastName (@username)" or just name if no username
   - Handle Chat type: return `sender.title`

2. Export `formatMessage(msg: Message): string`
   - Use `md.unparse(msg.textWithEntities)` from `@mtcute/markdown-parser` for text with formatting
   - Build output string with components:

   Format (per CONTEXT.md decisions):
   ```
   **[YYYY-MM-DD HH:MM:SS]** **Sender Name (@username)** [id:123]

   > Forwarded from: Source Name

   > In reply to [id:456]: "First 100 chars of original..."

   [Attachment: photo]

   Message text with **formatting** preserved

   ---
   ```

   - Handle forwards: Check `msg.forward`, get sender name (handle anonymous)
   - Handle replies: Check `msg.replyToMessage`, truncate `quoteText` to 100 chars with ellipsis
   - Handle attachments: Check `msg.media`, show type like "photo", "video", "document", etc.
   - Include message ID for reference: `[id:${msg.id}]`
   - Include reply-to ID if present: `[reply:${msg.replyToMessage.id}]` in the quote line

Key imports:
```typescript
import { Message, User, Chat } from '@mtcute/node'
import { md } from '@mtcute/markdown-parser'
```

Note: `AnonymousSender` may need to be imported from @mtcute/core if not exported from @mtcute/node.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
File exists at src/messages/format.ts
  </verify>
  <done>
formatMessage produces readable Markdown with sender, timestamp, reply quotes, forward attribution, attachment markers, and preserved text formatting
  </done>
</task>

<task type="auto">
  <name>Task 2: Create monthly file writer module</name>
  <files>src/messages/writer.ts</files>
  <action>
Create src/messages/writer.ts to handle monthly file structure and YAML frontmatter.

Implementation:

1. Export `groupByMonth(messages: Message[]): Map<string, Message[]>`
   - Group messages by YYYY-MM key from `msg.date`
   - Messages within each group should be sorted oldest-first (reverse of API order)

2. Export `createFrontmatter(chatName: string, chatId: number, firstMsgId: number, lastMsgId: number): string`
   - Return YAML frontmatter string:
   ```yaml
   ---
   chat_name: "Chat Name Here"
   chat_id: 123456
   first_message_id: 100
   last_message_id: 500
   exported_at: "2026-02-03T12:00:00.000Z"
   ---
   ```
   - Escape quotes in chat name with backslash

3. Export `writeMonthlyFiles(chatName: string, chatId: number, messages: Message[]): Promise<{ filesWritten: number; messagesWritten: number }>`
   - Group messages by month using groupByMonth
   - For each month:
     - Create directory `archive/YYYY-MM/` if needed
     - Sanitize chat name using `sanitizeFilename(chatName, chatId)` from `../utils/filename.js`
     - Build file path: `archive/YYYY-MM/{sanitized-name}.md`
     - Get first and last message IDs from the month's messages
     - Write frontmatter + all formatted messages (use formatMessage from ./format.js)
   - Return counts for progress reporting

Key imports:
```typescript
import { mkdirSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { Message } from '@mtcute/node'
import { sanitizeFilename } from '../utils/filename.js'
import { formatMessage } from './format.js'
```
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
File exists at src/messages/writer.ts
  </verify>
  <done>
writeMonthlyFiles creates archive/YYYY-MM/chat-name.md structure with YAML frontmatter and formatted messages in chronological order
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Both files exist and export their functions
3. format.ts uses md.unparse for entity-to-markdown conversion
4. writer.ts imports from Plan 01's filename.ts correctly
</verification>

<success_criteria>
- src/messages/format.ts exports formatMessage with full message formatting
- src/messages/writer.ts exports writeMonthlyFiles with frontmatter and monthly structure
- Messages are reversed to chronological order before writing
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-message-export/03-02-SUMMARY.md`
</output>
